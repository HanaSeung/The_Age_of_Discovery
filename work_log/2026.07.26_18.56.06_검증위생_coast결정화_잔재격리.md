# 2026.07.26 (C:) 검증 위생 — coast 결정화 + 07.18 잔재 격리

- **작업 폴더**: `C:\MyApp\The_Age_of_Discovery` (이 세션부터 C: 활성)
- **세션 시각**: 2026.07.26 저녁
- **비고**: 오전 D: 세션(fps 계기 → 컬링 → WebGL 1단계, 최종 100fps)의 후속.
  D: 세션 기록은 **D: 의 work_log** 에 있다 — `2026.07.26_10.00.00_프레임계기_육지컬링.md`.
  work_log 는 git 밖이라 두 기계의 기록이 갈라져 있다 (아래 4장).

---

## 1. C: 전환 확인

pull 여부를 파일 존재(earcut.min.js·verify_gl.js·verify_cull.js)로 확인 — 동기화됨.
이 기계의 PowerShell PATH 에 git 없음 (흔한 설치 경로에도 없음 — GUI 로 pull 한 듯).
읽기 확인이 필요한 세션에서는 git 경로를 먼저 물을 것.

## 2. verify_coast — "오락가락"의 진짜 정체는 세 겹이었다

목표는 seed 고정이었는데, 파 보니 겹겹이 있었다:

1. **표식 파손 (결정적 고장)**: 병렬 창이 절 제목을 `단일 Path2D → 고리별 Path2D` 로
   바꿨고 그게 커밋됨. verify_coast 의 절단 표식이 옛 제목이라 **커밋 시점부터
   '찾지 못함'으로 즉사**. 어제 D: 마지막 회귀의 coast FAIL 을 '표본 무작위'로
   치부했는데 절반은 오진이었다. → 표식을 앞머리(`// ===== 육지 폴리곤`)만으로 느슨하게
2. **껍데기 누락**: 디코더가 `landPath.addPath(rp)` 를 쓰게 됐는데 verify_coast 의
   Path2D 껍데기에 addPath 없음 (verify_land 만 고쳐져 있었다) → 추가
3. **표본 무작위 (본래 목표)**: `Math.random` 16곳 → mulberry32 고정 seed(20260726)
   `rnd()` 로 일괄 치환. 근거 주석 포함

결과: **23/23, 3회 실행 출력 해시 완전 동일** — 실패가 나면 매번 같은 표본에서 난다.

## 3. C: 에만 있던 07.18 잔재 — 격리

C: 전수 회귀에 D: 에 없던 실패 셋(`verify_bathy`·`verify_curviz`·`verify_speed`)이 등장.
전부 **2026.07.18 날짜의 미커밋 잔재**로, `D:\` 절대 경로가 박혀 C: 에서 열리지도 못함
(07.25 verify_scale 개정에서 지적된 그 병의 형제들). `_curstat.js`(07.18 프로브 잔재)와
함께 `_stale_2026.07.18\` 로 **이동(삭제 아님)**. 살릴 내용이 있으면 꺼내고, 없으면
아이삭이 폴더째 지우면 된다.

격리 후 전수 회귀: **실패 = 묵은 6 (`helm` `sail` `sailable` `ui` `windsail` `zoom`) 뿐.**
C: 기준선 확립.

## 4. 두 기계의 work_log 가 갈라져 있다 (미해결)

- D:: 9개 파일 + INDEX (07.18~07.26, 오늘 오전 대작업 포함)
- C:: 07.18 나침반카드·조타·패널UI 2개 파일뿐 — **D: 목록에 없는 세션이다** (07.19 공백의
  일부가 여기 있었을 가능성). 역으로 C: 에는 5~9번이 없다
- 해소 방안 후보: ① 수동 상호 복사(둘의 합집합을 양쪽에), ② work_log 를 .gitignore 에서
  빼고 저장소에 넣기(비공개 저장소이므로 가능, 단 로그가 커밋 이력을 탄다),
  ③ 한 기계를 기록 주인으로 지정 — **아이삭 결정 대기**

## 5. 미커밋 변경

`verify_coast.js` 수정 1건 (표식·addPath·seed). 커밋 초안:

```
verify_coast 소생 — 표식 파손·addPath 누락·표본 무작위 세 겹 수리

'단일→고리별' 절 개명에 절단 표식이 깨져 커밋 후 즉사 상태였고,
그 아래에 addPath 껍데기 누락, 그 아래에 seed 없는 표본이 있었다.
mulberry32(20260726) 고정 — 3회 실행 출력이 바이트 단위로 동일하다.
```

`_stale_2026.07.18\` 는 미추적 폴더라 커밋과 무관.

## 6. 세계 전체 화면 1fps — 내가 심은 결함과 수리

최소 배율 실측 **1fps** (아이삭). 예상(옛 수준 5~6)보다 나쁨 = 한계가 아니라 결함.
원인: `coastStrokePath` 가 세계 전체 화면에서 **격자 52만 칸 × 사본 3장을 다 돌아
15만 선분을 모아 놓고 나서야** "많다, 통짜로" 하고 버림 — 매 프레임 헛수고.
수리: `SEG_STROKE_CELLS`(60,000) — 걸친 칸 수를 순회 **전에** 재서 즉시 통짜 후퇴.
경도로 세계보다 넓게 보이면 spanX 를 CGW 로 접음. `verify_gl` 에 ★조기 포기 항목
추가(포기가 순회보다 앞에 있는지) — **26/26**. 전수 회귀 = 묵은 6 뿐.
재측정 대기: 최소 배율에서 5~6 수준 복귀가 예상값. 그 뒤 2단계(선-사각형 GPU stroke)
여부를 정한다.

커밋 초안 (5장 것과 합쳐 한 번에):

```
verify_coast 소생 + 세계 전체 화면 조기 포기

verify_coast: '단일→고리별' 절 개명에 절단 표식이 깨져 커밋 후 즉사 상태였고,
그 아래 addPath 껍데기 누락, 그 아래 seed 없는 표본. mulberry32(20260726) 고정
— 3회 실행 출력이 바이트 단위로 동일하다. 23/23.

coastStrokePath: 세계 전체 화면에서 52만 칸x3 을 다 돌아 15만 선분을 모은 뒤에야
버리던 헛수고(1fps 사고)를 SEG_STROKE_CELLS=60000 으로 순회 전에 돌려세움.
verify_gl 26/26 (조기 포기 ★항목 추가).
```

## 7. 이동 중 순간 멈춤 — 미니맵 굽기 컬링

증상: 배 이동 중 간헐적 끊김. **100fps 가 되며 드러난 문제** — 5~6fps 시절엔 모든
프레임이 느려 굽는 프레임의 튐(수십 ms)이 묻혔다 (07.26 오전 fps 설계 때 예측한 그것).
판정: 코드 없이 `M` 으로 미니맵을 끄자 끊김 소멸 — 범인 확정 (아이삭 실측).
원인: `doBake` 가 조각 사각형만 필요한데 **통짜 landPath 41만 점을 사본마다 fill+stroke**.
수리: `landFor(x0,y0,x1,y1)` — 조각과 겹치는 고리만 addPath (본지도 landCull 과 같은
원리·같은 자료, 구멍은 상자 포함으로 함께). LAND_RINGS 없으면 통짜 후퇴.
500km 단에서 굽기가 수천 점으로 줄어 튐 자체가 소멸. 승인받았던 프레임 분할+이중
버퍼는 이걸로 부족할 때(8,000km 단) 2차로 — 계획 축소.
`verify_minimap` 61→**64/64**: ★전수 대조·구멍 동반·통짜 금지·후퇴 항목으로 교체.
전수 회귀 = 묵은 6 뿐.

미해결 이관: **세계 전체 화면 1fps** — 조기 포기 수리로도 그대로 (내 가설 기각).
아이삭이 "다른 방식으로 해결" 선언, 범인 미확정 상태로 보류. 후보: 통짜 stroke
41만x2, GL 사본 3장 x 120만 정점, 3층 합성. 재개 시 구간 계측부터.

**기록 끝**
